name: Publish to npm

# Publishes when a version tag (e.g. v0.1.1) is pushed:
#   npm version patch && git push --follow-tags
#
# Uses npm OIDC trusted publishing — no NPM_TOKEN secret. One-time setup:
# npmjs.com → @apertis/agent → Settings → Trusted Publisher → GitHub Actions,
# repo apertis-ai/apertis-agent, workflow publish.yml.

on:
  push:
    tags: ["v*"]

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # required for OIDC trusted publishing
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          registry-url: "https://registry.npmjs.org"
      - run: npm ci
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm run test:run
      - name: Build
        run: npm run build
      - name: Upgrade npm for OIDC trusted publishing
        run: npm install -g npm@latest
      - name: Publish
        run: npm publish --access public
